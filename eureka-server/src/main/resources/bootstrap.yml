spring:
  profiles:
    active: dev

---
spring:
  profiles: dev
  application:
    name: eureka-server
server:
  port: 8060

---
spring:
  profiles: prod
  application:
    name: eureka-server
server:
  port: 80
